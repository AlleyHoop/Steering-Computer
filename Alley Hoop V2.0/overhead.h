#ifndef OVERHEAD_H_
#define OVERHEAD_H_

#include "IOPins.h"
#include "USART.h"

#define HIGH 1
#define LOW 0
#define constrain(amt,low,high) ((amt)<(low)?(low):((amt)>(high)?(high):(amt)))

#endif /* DEFINES_H_ */